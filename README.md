Adds support for Snockets (Node alternative to Sprockets) to DocPad

First working prototype.

If you want to test it, drop it in plugins/snockets in your docpad project directory.

Use #= require and #= require_tree